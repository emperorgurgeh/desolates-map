// TODO continue implementing

class Cluster {
  constructor(letter, number) {
    this.letter = letter;
    this.number = number;
  }

  toString = () => (`${this.letter}${this.number}`);

  containsPlanet(planet) {

      const A_ASCII = 97;
      let letterIdx = u1.toLowerCase().charCodeAt(0) - A_ASCII;
      let numberIdx = u2 - 1; // 0-index

      // let

      // for (i = -1000; i<= 1000; i += 2*1000/2) {}
  }

}
